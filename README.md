# homebrew-tap

Homebrew tap for [sluice](https://github.com/Pyronewbic/Sluice) - sandboxed, egress-firewalled
containers for any project and coding agent.

```bash
brew install Pyronewbic/tap/sluice
```

Needs **docker** or **podman** at runtime (Homebrew doesn't manage it); `sluice init` needs neither.
